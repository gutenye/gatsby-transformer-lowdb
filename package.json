{
  "name": "gatsby-transformer-lowdb",
  "version": "0.0.4",
  "description": "Gatsby transformer plugin for lwodb JSON files",
  "scripts": {
    "build": "babel src --out-dir . --ignore __tests__",
    "watch": "babel -w src --out-dir . --ignore __tests__",
    "prepublish": "npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "json",
    "lowdb"
  ],
  "author": "Guten Ye <ywzhaifei@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.0",
    "unist-util-select": "^1.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1"
  }
}
